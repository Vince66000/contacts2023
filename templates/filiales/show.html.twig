{% extends 'base.html.twig' %}

{% block title %}Filiales{% endblock %}

{% block body %}
    <h1>Filiales</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiale.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ filiale.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filiales_index') }}">back to list</a>

    <a href="{{ path('app_filiales_edit', {'id': filiale.id}) }}">edit</a>

    {{ include('filiales/_delete_form.html.twig') }}
{% endblock %}
