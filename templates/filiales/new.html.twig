{% extends 'base.html.twig' %}

{% block title %}Nouvelle filiale{% endblock %}

{% block body %}
    <h1 class="text-center p-5">Créer une nouvelle filiale</h1>
    {{ form_start(form) }}
    <div class="row justify-content-sm-center">
        <div class="col-1">
            <p class="text-center">{{ form_label(form.Nom) }}</p>
        </div>
        <div class="col-2">
            {{ form_widget(form.Nom) }}
        </div>
        <div class="col-1">{{ form_label(form.ID_Avensys) }}</div>
        <div class="col-2">{{ form_widget(form.ID_Avensys) }}</div>
        <div class="col-2">
            <button class="btn bg-primary-subtle" >{{ button_label | default('Enregistrer') }}</button>
            <a class="btn bg-warning-subtle" href="{{ path('index') }}">Retour</a>
        </div>

    </div>
    {{ form_end(form) }}

{% endblock %}
