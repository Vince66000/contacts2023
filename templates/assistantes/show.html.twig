{% extends 'base.html.twig' %}

{% block title %}Assistantes{% endblock %}

{% block body %}
    <h1>Assistantes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assistante.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ assistante.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assistantes_index') }}">back to list</a>

    <a href="{{ path('app_assistantes_edit', {'id': assistante.id}) }}">edit</a>

    {{ include('assistantes/_delete_form.html.twig') }}
{% endblock %}
